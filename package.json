{
  "name": "ts-runtime-validator",
  "version": "0.0.5",
  "description": "A Typescript transformer that generates runtime validation functions from type definitions.",
  "exports": {
    ".": "./dist/index.js",
    "./transformer": "./dist/transformer.js"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "clear-cache": "yarn jest --clearCache",
    "test": "yarn clear-cache && jest",
    "test:watch": "yarn clear-cache && jest --watch"
  },
  "author": "jrdrg",
  "license": "ISC",
  "peerDependencies": {
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.5",
    "ts-node": "^10.0.0",
    "ttypescript": "^1.5.12",
    "typescript": "^4.2.4"
  }
}
